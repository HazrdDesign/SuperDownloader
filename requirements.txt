# Pinned so every build is reproducible. Update deliberately, then run the tests.

# --- App -----------------------------------------------------------------------------------------
yt-dlp[default,curl-cffi]==2026.8.19   # the downloader engine (updatable at runtime, see app/updater.py)
yt-dlp-ejs==0.8.0                      # YouTube JavaScript challenge solver scripts (run by bundled Deno)
customtkinter==6.0.0
pillow==12.3.0
tkinterdnd2==0.6.3                     # drag and drop links onto the window

# yt-dlp's optional dependencies (pulled in by the [default,curl-cffi] extras), pinned
brotli==1.2.0
certifi==2026.7.22
curl_cffi==0.16.3
mutagen==1.48.1
pycryptodomex==3.23.0
requests==2.34.2
urllib3==2.8.0
websockets==17.1
charset-normalizer==3.5.1
idna==3.20
cffi==2.1.1
pycparser==3.0
darkdetect==0.8.0
packaging==26.3

# --- Build and test ------------------------------------------------------------------------------
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.5
pytest==9.1.1
pluggy==1.6.0
iniconfig==2.3.0
Pygments==2.21.0
