[pytest]
testpaths = tests
addopts = -q
# Dump every thread's stack if a single test runs longer than this (diagnoses hangs on CI).
faulthandler_timeout = 120
markers =
    network: needs internet access to real video sites (skipped unless VD_NETWORK_TESTS=1)
    gui: needs a display
